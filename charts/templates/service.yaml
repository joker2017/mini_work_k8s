apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.account.name }}-{{ .Values.environment }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.account.service.port }}
      targetPort: {{ .Values.account.port }}
  selector:
    app: {{ .Values.account.name }}
    environment: {{ .Values.environment }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.profile.name }}-{{ .Values.environment }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.profile.service.port }}
      targetPort: {{ .Values.profile.port }}
  selector:
    app: {{ .Values.profile.name }}
    environment: {{ .Values.environment }}