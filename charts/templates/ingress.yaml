apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "{{ .Values.app.name }}-ingress-{{ .Values.global.deploymentColor }}"
spec:
  rules:
    - host: "{{ .Values.ingress.host }}"
      http:
        paths:
          - path: "/account"
            pathType: Prefix
            backend:
              service:
                name: "{{ .Values.account.name }}-{{ .Values.global.deploymentColor }}"
                port:
                  number: {{ .Values.account.port }}
          - path: "/profile"
            pathType: Prefix
            backend:
              service:
                name: "{{ .Values.profile.name }}-{{ .Values.global.deploymentColor }}"
                port:
                  number: {{ .Values.profile.port }}
